# Core
typer>=0.9.0
rich>=13.7.0
pyyaml>=6.0
python-dotenv>=1.0.0

# System/Network (Python bindings)
stem>=1.8.0          # For controlling Tor
netifaces>=0.11.0    # For network interfaces
psutil>=5.9.0        # For system processes

# Browser Automation
playwright>=1.40.0
playwright-stealth>=1.0.0

# GUI (we'll use Tauri later via Rust, but keep PyQt for prototyping)
PyQt6>=6.5.0

# Testing & Dev
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
mypy>=1.0.0
